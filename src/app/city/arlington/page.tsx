import { Metadata } from "next";

export const metadata: Metadata = {
  title: "City of Arlington",
  description: "City page for Arlington, VA",
};

export default function ArlingtonPage() {
  return null;
}
